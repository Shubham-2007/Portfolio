{
  "name": "api-gateway",
  "version": "1.0.0",
  "description": "Single entry point — routes requests to downstream services",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "test": "node --test src/index.test.js"
  },
  "engines": {
    "node": ">=20"
  }
}
